{
  "_comment": "Copy this file to .mcp.json and uncomment the servers you need. Set required env vars in your shell or .claude/settings.local.json. Never commit .mcp.json if it contains secrets.",
  "mcpServers": {
    "filesystem": {
      "_description": "Read/write files outside the current workspace. Adjust the allowed directories.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/dir"]
    },
    "github": {
      "_description": "GitHub API access: issues, PRs, repos, code search. Requires GITHUB_TOKEN.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "postgres": {
      "_description": "Query a Postgres database read-only. Replace connection string.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://user:pass@localhost:5432/dbname"]
    },
    "mssql": {
      "_description": "SQL Server via community MCP package (lightweight, npx). Uses --key=value args (not env vars).",
      "command": "npx",
      "args": ["-y", "@fynet/mcp-mssql", "--server=localhost", "--database=mydb", "--user=sa", "--password=yourpass"]
    },
    "mssql-dab": {
      "_description": "SQL Server via Microsoft Data API Builder (official, enterprise). Requires .NET 8+ and `dotnet tool install -g Microsoft.DataApiBuilder`. Run `dab init` + `dab add` to configure entities first.",
      "type": "stdio",
      "command": "dab",
      "args": ["start", "--mcp-stdio", "role:anonymous", "--config", "./dab-config.json", "--LogLevel", "error"]
    },
    "postman": {
      "_description": "Access Postman collections, environments, and APIs. Requires POSTMAN_API_KEY (Settings → API Keys).",
      "command": "npx",
      "args": ["-y", "@postman/postman-mcp-server"],
      "env": {
        "POSTMAN_API_KEY": "${POSTMAN_API_KEY}"
      }
    },
    "puppeteer": {
      "_description": "Browser automation for UI testing and screenshots.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"]
    },
    "sentry": {
      "_description": "Read Sentry issues and stack traces. Requires SENTRY_AUTH_TOKEN.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sentry"],
      "env": {
        "SENTRY_AUTH_TOKEN": "${SENTRY_AUTH_TOKEN}",
        "SENTRY_ORG": "your-org"
      }
    },
    "slack": {
      "_description": "Read/post to Slack channels. Requires SLACK_BOT_TOKEN + SLACK_TEAM_ID.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_TEAM_ID": "${SLACK_TEAM_ID}"
      }
    },
    "linear": {
      "_description": "Linear API (issues, projects, cycles). Requires LINEAR_API_KEY (https://linear.app/settings/api). Note: package name varies — verify on npm/MCP registry; this is a popular community choice.",
      "command": "npx",
      "args": ["-y", "@tacticlaunch/mcp-linear"],
      "env": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    },
    "jira": {
      "_description": "Atlassian Jira (issues, sprints, boards). Requires JIRA_HOST (yourcompany.atlassian.net), JIRA_EMAIL, JIRA_API_TOKEN (https://id.atlassian.com/manage-profile/security/api-tokens). Verify package name on npm before use.",
      "command": "npx",
      "args": ["-y", "mcp-atlassian"],
      "env": {
        "JIRA_HOST": "${JIRA_HOST}",
        "JIRA_EMAIL": "${JIRA_EMAIL}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      }
    },
    "notion": {
      "_description": "Notion (pages, databases, blocks). Requires NOTION_TOKEN — internal integration token from https://www.notion.so/my-integrations. Share target pages with the integration.",
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_TOKEN": "${NOTION_TOKEN}"
      }
    },
    "redis": {
      "_description": "Redis (read/write keys). Replace connection string. Use redis:// or rediss:// for TLS.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-redis", "redis://localhost:6379"]
    }
  }
}
